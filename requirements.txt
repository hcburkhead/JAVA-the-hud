# Core voice processing (Uses ADA's architecture from Naz Louis)
RealtimeSTT==0.3.7
RealtimeTTS==0.4.3

# LLM's
openai>=1.0.0
anthropic>=0.25.0
google-generativeai>=0.8.0
ollama>=0.3.0

# Optional: High-quality TTS
elevenlabs>=1.0.0

# System utilities
python-dotenv>=1.0.0

# Platform-specific
PyObjC==10.1; sys_platform == 'darwin'
